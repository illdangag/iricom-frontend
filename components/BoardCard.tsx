// react
import { Card, } from '@chakra-ui/react';
import { Board, } from '../interfaces';

type Props = {
  board: Board,
};

const BoardCard = ({
  board,
}: Props) => {
  return (
    <Card>
      TEST
    </Card>
  );
};

export default BoardCard;
